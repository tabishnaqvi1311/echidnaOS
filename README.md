## Running Locally
- Clone the repo
    `git clone https://github.com/tabishnaqvi1311/echidnaOS.git`

- Navigate to directory
    `cd echidnaOS`

-  Install qemu (if not installed)
    `brew install qemu` (for macOS)

- Run `qemu-system-i386 -fda build/main_floppy.img`
